---
const TIMESTAMP_END = 1777662000000;
---

<section class="flex flex-col gap-y-2 sm:gap-y-3 justify-center items-center">
  <div
    class="flex flex-row gap-x-2 sm:gap-x-3 md:gap-x-4 uppercase font-semibold"
    data-date={TIMESTAMP_END}
  >
    <div
      class="flex flex-col justify-center items-center bg-gradient-to-br from-pink-300 to-pink-500 rounded-xl p-1.5 sm:p-6 border-2 border-white shadow-lg gap-y-0.5 w-12 sm:w-14 md:w-20 transform hover:scale-105 transition-transform"
    >
      <span
        class="text-lg sm:text-xl md:text-4xl font-bold text-white drop-shadow-md leading-none"
        data-days>00</span
      >
      <span class="text-xs font-semibold text-white leading-tight">Días</span>
    </div>

    <span
      aria-hidden="true"
      class="flex items-center mt-1 text-lg sm:text-xl md:text-2xl text-white font-bold drop-shadow-md"
      >:</span
    >

    <div
      class="flex flex-col justify-center items-center bg-gradient-to-br from-pink-400 to-pink-600 rounded-xl p-1.5 sm:p-2 border-2 border-white shadow-lg gap-y-0.5 w-12 sm:w-14 md:w-20 transform hover:scale-105 transition-transform"
    >
      <span
        class="text-lg sm:text-xl md:text-4xl font-bold text-white drop-shadow-md leading-none"
        data-hours>00</span
      >
      <span class="text-xs font-semibold text-white leading-tight">Horas</span>
    </div>

    <span
      aria-hidden="true"
      class="flex items-center mt-1 text-lg sm:text-xl md:text-2xl text-white font-bold drop-shadow-md"
      >:</span
    >

    <div
      class="flex flex-col justify-center items-center bg-gradient-to-br from-pink-500 to-rose-600 rounded-xl p-1.5 sm:p-2 border-2 border-white shadow-lg gap-y-0.5 w-12 sm:w-14 md:w-20 transform hover:scale-105 transition-transform"
    >
      <span
        class="flex items-center mt-1 text-lg sm:text-xl md:text-4xl font-bold text-white drop-shadow-md leading-none"
        data-minutes>00</span
      >
      <span class="text-xs font-semibold text-white leading-tight">Minutos</span
      >
    </div>

    <span
      aria-hidden="true"
      class="flex items-center mt-1 text-lg sm:text-xl md:text-2xl text-white font-bold drop-shadow-md"
      >:</span
    >

    <div
      class="flex flex-col justify-center items-center bg-gradient-to-br from-pink-500 to-pink-700 rounded-xl p-1.5 sm:p-2 border-2 border-white shadow-lg gap-y-0.5 w-12 sm:w-14 md:w-20 transform hover:scale-105 transition-transform"
    >
      <span
        class="text-lg sm:text-xl md:text-4xl font-bold text-white drop-shadow-md leading-none"
        data-seconds>00</span
      >
      <span class="text-xs font-semibold text-white leading-tight"
        >Segundos</span
      >
    </div>
  </div>
</section>

<script>
  import type Countdown from "./Countdown.astro";

  const SECOND = 1000;
  const MINUTE = 60 * SECOND;
  const HOUR = 60 * MINUTE;
  const DAY = 24 * HOUR;

  function init() {
    const $countdown = document.querySelector("[data-date]");
    if (!$countdown) return;
    const $days = document.querySelector("[data-days]");
    const $hours = document.querySelector("[data-hours]");
    const $minutes = document.querySelector("[data-minutes]");
    const $seconds = document.querySelector("[data-seconds]");

    const timestamp = $countdown.getAttribute("data-date");
    if (!timestamp) return;

    const date = new Date(+timestamp).getTime();

    const formaTime = (time: number) => {
      return Math.floor(time).toString().padStart(2, "0");
    };

    function updateCountdown() {
      const now = Date.now();
      const diff = date - now;

      if ($days instanceof HTMLElement) {
        $days.innerText = formaTime(diff / DAY);
      }

      if ($hours instanceof HTMLElement) {
        $hours.innerText = formaTime((diff % DAY) / HOUR);
      }

      if ($minutes instanceof HTMLElement) {
        $minutes.innerText = formaTime((diff % HOUR) / MINUTE);
      }

      if ($seconds instanceof HTMLElement) {
        $seconds.innerText = formaTime((diff % MINUTE) / SECOND);
      }
    }
    setInterval(updateCountdown, SECOND);
  }
  init();
</script>
